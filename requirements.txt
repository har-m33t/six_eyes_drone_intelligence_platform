# Versions bumped from the README spec to ones with Python 3.13 wheels.
# numpy 2.x changed the ABI, so opencv/ultralytics are bumped to builds
# compiled against numpy 2.x. (Spec pins: opencv 4.9.0.80, ultralytics 8.2.0,
# numpy 1.26.4 — none ship cp313 wheels.)
opencv-python>=4.10.0.84
ultralytics>=8.3.0
websockets==12.0
requests==2.31.0
numpy>=2.1.0
python-dotenv==1.0.0
